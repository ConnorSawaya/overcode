---
title: CLI
description: Параметры и команда overcode CLI.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

CLI overcode по умолчанию запускает [TUI](/docs/tui) при запуске без каких-либо аргументов.

```bash
overcode
```

Но он также принимает команды, описанные на этой странице. Это позволяет вам программно взаимодействовать с overcode.

```bash
overcode run "Explain how closures work in JavaScript"
```

---

### tui

Запустите TUI overcode.

```bash
overcode [project]
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                      |
| ---------------------------------------- | -------- | ----------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                    |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                          |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |          | Промпт для использования                                                      |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                            |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт для прослушивания                                                        |
| <nobr><code>{"--hostname"}</code></nobr> |          | Имя хоста для прослушивания                                                   |

---

## Команды

CLI overcode также имеет следующие команды.

---

### agent

Управляйте агентами для overcode.

```bash
overcode agent [command]
```

---

### attach

Подключите терминал к уже работающему внутреннему серверу overcode, запущенному с помощью команд `serve` или `web`.

```bash
overcode attach [url]
```

Это позволяет использовать TUI с удаленным сервером overcode. Например:

```bash
# Start the backend server for web/mobile access
overcode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
overcode attach http://10.20.30.40:4096
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--dir"}</code></nobr>      |          | Рабочий каталог для запуска TUI                                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Создать ответвление сеанса при продолжении (используйте с `--continue` или `--session`)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `OVERCODE_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `OVERCODE_SERVER_USERNAME` или `overcode`) |

---

#### create

Создайте нового агента с пользовательской конфигурацией.

```bash
overcode agent create
```

Эта команда поможет вам создать новый агент с настраиваемой системной подсказкой и настройкой инструмента.

---

#### list

Перечислите всех доступных агентов.

```bash
overcode agent list
```

---

### auth

Команда для управления учетными данными и входом в систему для провайдеров.

```bash
overcode auth [command]
```

---

#### login

overcode использует список провайдеров с [Models.dev](https://models.dev), поэтому вы можете использовать `overcode auth login` для настройки ключей API для любого поставщика, которого вы хотите использовать. Это хранится в `~/.local/share/overcode/auth.json`.

```bash
overcode auth login
```

Когда overcode запускается, он загружает поставщиков из файла учетных данных. И если в ваших средах определены какие-либо ключи или файл `.env` в вашем проекте.

---

#### list

Перечисляет всех проверенных поставщиков, которые хранятся в файле учетных данных.

```bash
overcode auth list
```

Или короткая версия.

```bash
overcode auth ls
```

---

#### logout

Выключает вас из провайдера, удаляя его из файла учетных данных.

```bash
overcode auth logout
```

---

### github

Управляйте агентом GitHub для автоматизации репозитория.

```bash
overcode github [command]
```

---

#### install

Установите агент GitHub в свой репозиторий.

```bash
overcode github install
```

Это настроит необходимый рабочий процесс GitHub Actions и проведет вас через процесс настройки. [Подробнее](/docs/github).

---

#### run

Запустите агент GitHub. Обычно это используется в действиях GitHub.

```bash
overcode github run
```

##### Флаги

| Флаг                                  | Описание                                      |
| ------------------------------------- | --------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Имитирующее событие GitHub для запуска агента |
| <nobr><code>{"--token"}</code></nobr> | Токен личного доступа GitHub                  |

---

### mcp

Управляйте серверами протокола контекста модели.

```bash
overcode mcp [command]
```

---

#### add

Добавьте сервер MCP в свою конфигурацию.

```bash
overcode mcp add
```

Эта команда поможет вам добавить локальный или удаленный сервер MCP.

---

#### list

Перечислите все настроенные серверы MCP и состояние их подключения.

```bash
overcode mcp list
```

Или используйте короткую версию.

```bash
overcode mcp ls
```

---

#### auth

Аутентификация с помощью сервера MCP с поддержкой OAuth.

```bash
overcode mcp auth [name]
```

Если вы не укажете имя сервера, вам будет предложено выбрать один из доступных серверов с поддержкой OAuth.

Вы также можете перечислить серверы с поддержкой OAuth и их статус аутентификации.

```bash
overcode mcp auth list
```

Или используйте короткую версию.

```bash
overcode mcp auth ls
```

---

#### logout

Удалите учетные данные OAuth для сервера MCP.

```bash
overcode mcp logout [name]
```

---

#### debug

Отладка проблем с подключением OAuth для сервера MCP.

```bash
overcode mcp debug <name>
```

---

### models

Перечислите все доступные модели от настроенных поставщиков.

```bash
overcode models [provider]
```

Эта команда отображает все модели, доступные у настроенных вами поставщиков, в формате `provider/model`.

Это полезно для определения точного названия модели, которое будет использоваться в [вашем config](/docs/config/).

При желании вы можете передать идентификатор поставщика, чтобы фильтровать модели по этому поставщику.

```bash
overcode models anthropic
```

#### Флаги

| Флаг                                    | Описание                                                                          |
| --------------------------------------- | --------------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Обновите кеш моделей на сайте models.dev.                                         |
| <nobr><code>{"--verbose"}</code></nobr> | Используйте более подробный вывод модели (включая метаданные, такие как затраты). |

Используйте флаг `--refresh` для обновления списка кэшированных моделей. Это полезно, когда к поставщику добавлены новые модели и вы хотите увидеть их в overcode.

```bash
overcode models --refresh
```

---

### run

Запустите overcode в неинтерактивном режиме, передав приглашение напрямую.

```bash
overcode run [message..]
```

Это полезно для создания сценариев, автоматизации или когда вам нужен быстрый ответ без запуска полного TUI. Например.

```bash "overcode run"
overcode run Explain the use of context in Go
```

Вы также можете подключиться к работающему экземпляру `overcode serve`, чтобы избежать холодной загрузки сервера MCP при каждом запуске:

```bash
# Start a headless server in one terminal
overcode serve

# In another terminal, run commands that attach to it
overcode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Флаги

| Флаг                                     | Короткий | Описание                                                                                         |
| ---------------------------------------- | -------- | ------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--command"}</code></nobr>  |          | Команда для запуска, используйте сообщение для аргументов                                        |
| <nobr><code>{"--continue"}</code></nobr> | `-c`     | Продолжить последний сеанс                                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s`     | Идентификатор сеанса для продолжения                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |          | Разветвить сеанс при продолжении (используйте с `--continue` или `--session`)                    |
| <nobr><code>{"--share"}</code></nobr>    |          | Поделиться сеансом                                                                               |
| <nobr><code>{"--model"}</code></nobr>    | `-m`     | Модель для использования в виде поставщика/модели.                                               |
| <nobr><code>{"--agent"}</code></nobr>    |          | Агент для использования                                                                          |
| <nobr><code>{"--file"}</code></nobr>     | `-f`     | Файл(ы) для прикрепления к сообщению                                                             |
| <nobr><code>{"--format"}</code></nobr>   |          | Формат: по умолчанию (отформатированный) или json (необработанные события JSON).                 |
| <nobr><code>{"--title"}</code></nobr>    |          | Название сеанса (использует усеченное приглашение, если значение не указано)                     |
| <nobr><code>{"--attach"}</code></nobr>   |          | Подключитесь к работающему серверу overcode (например, http://localhost:4096)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p`     | Пароль базовой аутентификации (по умолчанию `OVERCODE_SERVER_PASSWORD`)                          |
| <nobr><code>{"--username"}</code></nobr> | `-u`     | Имя пользователя базовой аутентификации (по умолчанию `OVERCODE_SERVER_USERNAME` или `overcode`) |
| <nobr><code>{"--dir"}</code></nobr>      |          | Каталог для выполнения или путь на удалённом сервере при подключении                             |
| <nobr><code>{"--variant"}</code></nobr>  |          | Вариант модели (уровень рассуждений для провайдера)                                              |
| <nobr><code>{"--thinking"}</code></nobr> |          | Показать блоки размышлений                                                                       |
| <nobr><code>{"--port"}</code></nobr>     |          | Порт локального сервера (по умолчанию случайный порт)                                            |

---

### serve

Запустите автономный сервер overcode для доступа к API. Полный HTTP-интерфейс можно найти в [server docs](/docs/server).

```bash
overcode serve
```

При этом запускается HTTP-сервер, который обеспечивает доступ API к функциям overcode без интерфейса TUI. Установите `OVERCODE_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `overcode`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### session

Управляйте сессиями overcode.

```bash
overcode session [command]
```

---

#### list

Перечислите все сеансы overcode.

```bash
overcode session list
```

##### Флаги

| Флаг                                      | Короткий | Описание                                  |
| ----------------------------------------- | -------- | ----------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`     | Ограничить N последних сеансов.           |
| <nobr><code>{"--format"}</code></nobr>    |          | Формат вывода: таблица или json (таблица) |

---

### stats

Покажите статистику использования токенов и затрат для ваших сеансов overcode.

```bash
overcode stats
```

#### Флаги

| Флаг                                    | Описание                                                                                                   |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Показать статистику за последние N дней (все время)                                                        |
| <nobr><code>{"--tools"}</code></nobr>   | Количество инструментов для отображения (все)                                                              |
| <nobr><code>{"--models"}</code></nobr>  | Показать разбивку по использованию модели (по умолчанию скрыто). Передайте номер, чтобы показать верхнюю N |
| <nobr><code>{"--project"}</code></nobr> | Фильтровать по проекту (все проекты, пустая строка: текущий проект)                                        |

---

### export

Экспортируйте данные сеанса в формате JSON.

```bash
overcode export [sessionID]
```

Если вы не укажете идентификатор сеанса, вам будет предложено выбрать один из доступных сеансов.

---

### import

Импортируйте данные сеанса из файла JSON или URL-адреса общего ресурса overcode.

```bash
overcode import <file>
```

Вы можете импортировать из локального файла или URL-адреса общего ресурса overcode.

```bash
overcode import session.json
overcode import https://opncd.ai/s/abc123
```

---

### web

Запустите автономный сервер overcode с веб-интерфейсом.

```bash
overcode web
```

При этом запускается HTTP-сервер и открывается веб-браузер для доступа к overcode через веб-интерфейс. Установите `OVERCODE_SERVER_PASSWORD`, чтобы включить базовую аутентификацию HTTP (имя пользователя по умолчанию — `overcode`).

#### Флаги

| Флаг                                     | Описание                                                      |
| ---------------------------------------- | ------------------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания                                        |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания                                   |
| <nobr><code>{"--mdns"}</code></nobr>     | Включить обнаружение mDNS                                     |
| <nobr><code>{"--cors"}</code></nobr>     | Дополнительные источники браузера, позволяющие разрешить CORS |

---

### acp

Запустите сервер ACP (агент-клиентский протокол).

```bash
overcode acp
```

Эта команда запускает сервер ACP, который обменивается данными через stdin/stdout с использованием nd-JSON.

#### Флаги

| Флаг                                     | Описание                    |
| ---------------------------------------- | --------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Рабочий каталог             |
| <nobr><code>{"--port"}</code></nobr>     | Порт для прослушивания      |
| <nobr><code>{"--hostname"}</code></nobr> | Имя хоста для прослушивания |

---

### uninstall

Удалите overcode и удалите все связанные файлы.

```bash
overcode uninstall
```

#### Флаги

| Флаг                                        | Короткий | Описание                                   |
| ------------------------------------------- | -------- | ------------------------------------------ |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`     | Сохраняйте файлы конфигурации              |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`     | Храните данные сеанса и снимки             |
| <nobr><code>{"--dry-run"}</code></nobr>     |          | Покажите, что было бы удалено без удаления |
| <nobr><code>{"--force"}</code></nobr>       | `-f`     | Пропустить запросы подтверждения           |

---

### upgrade

Обновляет overcode до последней версии или определенной версии.

```bash
overcode upgrade [target]
```

Чтобы обновиться до последней версии.

```bash
overcode upgrade
```

Для обновления до определенной версии.

```bash
overcode upgrade v0.1.48
```

#### Флаги

| Флаг                                   | Короткий | Описание                                                  |
| -------------------------------------- | -------- | --------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`     | Используемый метод установки: local, npm, pnpm, bun, brew |

---

## Глобальные флаги

CLI overcode принимает следующие глобальные флаги.

| Флаг                                       | Короткий | Описание                                   |
| ------------------------------------------ | -------- | ------------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`     | Отобразить справку                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`     | Распечатать номер версии                   |
| <nobr><code>{"--print-logs"}</code></nobr> |          | Печать журналов в stderr                   |
| <nobr><code>{"--log-level"}</code></nobr>  |          | Уровень журнала (DEBUG, INFO, WARN, ERROR) |

---

## Переменные среды

overcode можно настроить с помощью переменных среды.

| Переменная                            | Тип                 | Описание                                                                         |
| ------------------------------------- | ------------------- | -------------------------------------------------------------------------------- |
| `OVERCODE_AUTO_SHARE`                 | логическое значение | Автоматически делиться сеансами                                                  |
| `OVERCODE_GIT_BASH_PATH`              | строка              | Путь к исполняемому файлу Git Bash в Windows                                     |
| `OVERCODE_CONFIG`                     | строка              | Путь к файлу конфигурации                                                        |
| `OVERCODE_TUI_CONFIG`                 | строка              | Путь к файлу конфигурации TUI                                                    |
| `OVERCODE_CONFIG_DIR`                 | строка              | Путь к каталогу конфигурации                                                     |
| `OVERCODE_CONFIG_CONTENT`             | строка              | Встроенное содержимое конфигурации json                                          |
| `OVERCODE_DISABLE_AUTOUPDATE`         | логическое значение | Отключить автоматическую проверку обновлений                                     |
| `OVERCODE_DISABLE_PRUNE`              | логическое значение | Отключить удаление старых данных                                                 |
| `OVERCODE_DISABLE_TERMINAL_TITLE`     | логическое значение | Отключить автоматическое обновление заголовка терминала                          |
| `OVERCODE_PERMISSION`                 | строка              | Встроенная конфигурация разрешений json                                          |
| `OVERCODE_DISABLE_DEFAULT_PLUGINS`    | логическое значение | Отключить плагины по умолчанию                                                   |
| `OVERCODE_DISABLE_LSP_DOWNLOAD`       | логическое значение | Отключить автоматическую загрузку LSP-сервера                                    |
| `OVERCODE_ENABLE_EXPERIMENTAL_MODELS` | логическое значение | Включить экспериментальные модели                                                |
| `OVERCODE_DISABLE_AUTOCOMPACT`        | логическое значение | Отключить автоматическое сжатие контекста                                        |
| `OVERCODE_DISABLE_CLAUDE_CODE`        | логическое значение | Отключить чтение из `.claude` (подсказка + навыки)                               |
| `OVERCODE_DISABLE_CLAUDE_CODE_PROMPT` | логическое значение | Отключить чтение `~/.claude/CLAUDE.md`                                           |
| `OVERCODE_DISABLE_CLAUDE_CODE_SKILLS` | логическое значение | Отключить загрузку `.claude/skills`                                              |
| `OVERCODE_DISABLE_MODELS_FETCH`       | логическое значение | Отключить получение моделей из удаленных источников                              |
| `OVERCODE_FAKE_VCS`                   | строка              | Поддельный поставщик VCS для целей тестирования                                  |
| `OVERCODE_CLIENT`                     | строка              | Идентификатор клиента (по умолчанию `cli`)                                       |
| `OVERCODE_ENABLE_EXA`                 | логическое значение | Включить инструменты веб-поиска Exa                                              |
| `OVERCODE_SERVER_PASSWORD`            | строка              | Включить базовую аутентификацию для `serve`/`web`                                |
| `OVERCODE_SERVER_USERNAME`            | строка              | Переопределить имя пользователя базовой аутентификации (по умолчанию `overcode`) |
| `OVERCODE_MODELS_URL`                 | строка              | Пользовательский URL-адрес для получения конфигурации модели                     |

---

### Экспериментальные функции

Эти переменные среды позволяют использовать экспериментальные функции, которые могут быть изменены или удалены.

| Переменная                                      | Тип                 | Описание                                               |
| ----------------------------------------------- | ------------------- | ------------------------------------------------------ |
| `OVERCODE_EXPERIMENTAL`                         | логическое значение | Включить экспериментальные функции под общим флагом    |
| `OVERCODE_EXPERIMENTAL_ICON_DISCOVERY`          | логическое значение | Включить обнаружение значков                           |
| `OVERCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | логическое значение | Отключить копирование при выборе в TUI                 |
| `OVERCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | число               | Таймаут по умолчанию для команд bash в мс              |
| `OVERCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | число               | Максимальное количество токенов вывода для ответов LLM |
| `OVERCODE_EXPERIMENTAL_FILEWATCHER`             | логическое значение | Включить просмотр файлов для всего каталога            |
| `OVERCODE_EXPERIMENTAL_OXFMT`                   | логическое значение | Включить форматтер oxfmt                               |
| `OVERCODE_EXPERIMENTAL_LSP_TOOL`                | логическое значение | Включить экспериментальный инструмент LSP              |
| `OVERCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | логическое значение | Отключить просмотрщик файлов                           |
| `OVERCODE_EXPERIMENTAL_EXA`                     | логическое значение | Включить экспериментальные функции Exa                 |
| `OVERCODE_EXPERIMENTAL_LSP_TY`                  | логическое значение | Включить TY LSP для файлов python                      |
| `OVERCODE_EXPERIMENTAL_PLAN_MODE`               | логическое значение | Включить режим плана                                   |
| `OVERCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | логическое значение | Включить фоновые задачи субагентов                     |
| `OVERCODE_EXPERIMENTAL_EVENT_SYSTEM`            | логическое значение | Включить экспериментальную систему событий             |
| `OVERCODE_EXPERIMENTAL_NATIVE_LLM`              | логическое значение | Включить нативный путь запросов LLM                    |
| `OVERCODE_EXPERIMENTAL_PARALLEL`                | логическое значение | Включить параллельное выполнение веб-поиска            |
| `OVERCODE_EXPERIMENTAL_SCOUT`                   | логическое значение | Включить субагент Scout                                |
| `OVERCODE_EXPERIMENTAL_WORKSPACES`              | логическое значение | Включить поддержку рабочих областей                    |
