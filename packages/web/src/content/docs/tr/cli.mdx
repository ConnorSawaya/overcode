---
title: CLI
description: overcode CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

overcode CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
overcode
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, overcode ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
overcode run "Explain how closures work in JavaScript"
```

---

### tui

overcode terminal kullanıcı arayüzünü başlatır.

```bash
overcode [project]
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                    |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                        |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                 |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Kullanılacak prompt                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak agent                                                          |
| <nobr><code>{"--port"}</code></nobr>     |      | Dinlenecek port                                                             |
| <nobr><code>{"--hostname"}</code></nobr> |      | Dinlenecek host adı                                                         |

---

## Komutlar

overcode CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

overcode için agent'ları yönetin.

```bash
overcode agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir overcode arka uç sunucusuna bir terminal ekleyin.

```bash
overcode attach [url]
```

Bu, TUI öğesinin uzak bir overcode arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
overcode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
overcode attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak                                   | Kısa | Tanım                                                                                         |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | TUI'yi başlatmak için çalışma dizini                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum açma bilgileri                                                            |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu çatalla (`--continue` veya `--session` ile kullanın)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `OVERCODE_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `OVERCODE_SERVER_USERNAME` veya `overcode`) |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
overcode agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
overcode agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
overcode auth [command]
```

---

#### login

overcode, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `overcode auth login` kullanabilirsiniz. Bu `~/.local/share/overcode/auth.json`'de saklanıyor.

```bash
overcode auth login
```

overcode başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
overcode auth list
```

Veya kısa versiyonu.

```bash
overcode auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
overcode auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
overcode github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
overcode github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
overcode github run
```

##### Bayraklar

| Bayrak                                | Açıklama                                    |
| ------------------------------------- | ------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Aracıyı çalıştırmak için GitHub sahte olayı |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
overcode mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
overcode mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
overcode mcp list
```

Veya kısa versiyonunu kullanın.

```bash
overcode mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
overcode mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
overcode mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
overcode mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
overcode mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
overcode mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
overcode models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
overcode models anthropic
```

#### Bayraklar

| Bayrak                                  | Tanım                                                                       |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Modeller.dev'den model önbelleğini yenileyin                                |
| <nobr><code>{"--verbose"}</code></nobr> | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları overcode'da görmek istediğinizde kullanışlıdır.

```bash
overcode models --refresh
```

---

### run

Doğrudan bir istem ileterek overcode'u etkileşimli olmayan modda çalıştırın.

```bash
overcode run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "overcode run"
overcode run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `overcode serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
overcode serve

# In another terminal, run commands that attach to it
overcode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                                      |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Çalıştırılacak komut, args için mesajı kullanın                                               |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)                   |
| <nobr><code>{"--share"}</code></nobr>    |      | Oturumu paylaşın                                                                              |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak temsilci                                                                         |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Mesaja eklenecek dosya(lar)                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                            |
| <nobr><code>{"--title"}</code></nobr>    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)                      |
| <nobr><code>{"--attach"}</code></nobr>   |      | Çalışan bir overcode sunucusuna ekleyin (ör. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `OVERCODE_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `OVERCODE_SERVER_USERNAME` veya `overcode`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Çalıştırılacak dizin veya bağlanırken uzak sunucudaki yol                                     |
| <nobr><code>{"--variant"}</code></nobr>  |      | Model varyantı (sağlayıcıya özgü muhakeme düzeyi)                                             |
| <nobr><code>{"--thinking"}</code></nobr> |      | Düşünme bloklarını göster                                                                     |
| <nobr><code>{"--port"}</code></nobr>     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır)             |

---

### serve

API erişimi için headless bir overcode sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
overcode serve
```

Bu, TUI arayüzü olmadan overcode işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `OVERCODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `overcode` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

overcode oturumlarını yönetin.

```bash
overcode session [command]
```

---

#### list

Tüm overcode oturumlarını listeleyin.

```bash
overcode session list
```

##### Bayraklar

| Bayrak                                    | Kısa | Tanım                                  |
| ----------------------------------------- | ---- | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | En son N oturumla sınırla              |
| <nobr><code>{"--format"}</code></nobr>    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

overcode oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
overcode stats
```

#### Bayraklar

| Bayrak                                  | Açıklama                                                                                                    |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Gösterilecek araç sayısı (tümü)                                                                             |
| <nobr><code>{"--models"}</code></nobr>  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| <nobr><code>{"--project"}</code></nobr> | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
overcode export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya overcode paylaşımından URL oturum verilerini içe aktarın.

```bash
overcode import <file>
```

Yerel bir dosyadan veya overcode paylaşımından (URL) içe aktarabilirsiniz.

```bash
overcode import session.json
overcode import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir overcode sunucusu başlatın.

```bash
overcode web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla overcode'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `OVERCODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `overcode` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
overcode acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak                                   | Açıklama            |
| ---------------------------------------- | ------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Çalışma dizini      |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek port     |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek host adı |

---

### uninstall

overcode'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
overcode uninstall
```

#### Bayraklar

| Bayrak                                      | Kısa | Tanım                                           |
| ------------------------------------------- | ---- | ----------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Yapılandırma dosyalarını sakla                  |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Onay istemlerini atla                           |

---

### upgrade

overcode'u en son sürüme veya belirli bir sürüme günceller.

```bash
overcode upgrade [target]
```

En son sürüme yükseltmek için.

```bash
overcode upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
overcode upgrade v0.1.48
```

#### Bayraklar

| Bayrak                                 | Kısa | Açıklama                                               |
| -------------------------------------- | ---- | ------------------------------------------------------ |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

overcode CLI aşağıdaki global bayrakları destekler.

| Bayrak                                     | Kısa | Tanım                                    |
| ------------------------------------------ | ---- | ---------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Yardımı görüntüle                        |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Sürüm numarasını yazdır                  |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Günlükleri stderr'e yazdır               |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

overcode ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `OVERCODE_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `OVERCODE_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `OVERCODE_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `OVERCODE_TUI_CONFIG`                 | string  | TUI yapılandırma dosyasının yolu                                            |
| `OVERCODE_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `OVERCODE_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `OVERCODE_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `OVERCODE_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `OVERCODE_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `OVERCODE_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `OVERCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `OVERCODE_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `OVERCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `OVERCODE_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `OVERCODE_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `OVERCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `OVERCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `OVERCODE_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `OVERCODE_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `OVERCODE_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `OVERCODE_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `OVERCODE_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `OVERCODE_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `overcode`) |
| `OVERCODE_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `OVERCODE_EXPERIMENTAL`                         | boolean | Şemsiye bayrağa bağlı deneysel özellikleri etkinleştir  |
| `OVERCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `OVERCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `OVERCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `OVERCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `OVERCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `OVERCODE_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `OVERCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `OVERCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `OVERCODE_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `OVERCODE_EXPERIMENTAL_LSP_TY`                  | boolean | python dosyaları için TY LSP'yi etkinleştir             |
| `OVERCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
| `OVERCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Arka plan alt ajan görevlerini etkinleştir              |
| `OVERCODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Deneysel olay sistemini etkinleştir                     |
| `OVERCODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Yerel LLM istek yolunu etkinleştir                      |
| `OVERCODE_EXPERIMENTAL_PARALLEL`                | boolean | Paralel web araması yürütmesini etkinleştir             |
| `OVERCODE_EXPERIMENTAL_SCOUT`                   | boolean | Scout alt ajanını etkinleştir                           |
| `OVERCODE_EXPERIMENTAL_WORKSPACES`              | boolean | Çalışma alanı desteğini etkinleştir                     |
