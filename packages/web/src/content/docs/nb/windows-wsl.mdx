---
title: Windows (WSL)
description: Kjør Overcode på Windows med WSL for best opplevelse.
---

import { Steps } from "@astrojs/starlight/components"

Selv om Overcode kan kjøre direkte på Windows, anbefaler vi å bruke [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install) for best mulig opplevelse. WSL gir et Linux-miljø som fungerer godt med funksjonene i Overcode.

:::tip[Hvorfor WSL?]
WSL gir bedre filsystemytelse, full terminalstøtte og kompatibilitet med utviklingsverktøyene Overcode er avhengig av.
:::

---

## Oppsett

<Steps>

1. **Installer WSL**

   Hvis du ikke har gjort det ennå, [installer WSL](https://learn.microsoft.com/en-us/windows/wsl/install) med den offisielle guiden fra Microsoft.

2. **Installer Overcode i WSL**

   Når WSL er satt opp, åpner du WSL-terminalen og installerer Overcode med en av [installasjonsmetodene](/docs/).

   ```bash
   curl -fsSL https://raw.githubusercontent.com/ConnorSawaya/overcode/overcode-rebrand/install | bash
   ```

3. **Bruk Overcode fra WSL**

   Gå til prosjektmappen din (Windows-filer er tilgjengelige via `/mnt/c/`, `/mnt/d/` osv.) og kjør Overcode.

   ```bash
   cd /mnt/c/Users/YourName/project
   overcode
   ```

</Steps>

---

## Skrivebordsapp + WSL-server

Hvis du foretrekker Overcode-skrivebordsappen, men vil kjøre serveren i WSL:

1. **Start serveren i WSL** med `--hostname 0.0.0.0` for å tillate eksterne tilkoblinger:

   ```bash
   overcode serve --hostname 0.0.0.0 --port 4096
   ```

2. **Koble skrivebordsappen** til `http://localhost:4096`

:::note
Hvis `localhost` ikke fungerer i oppsettet ditt, bruk WSL-IP-adressen i stedet (fra WSL: `hostname -I`) og koble til `http://<wsl-ip>:4096`.
:::

:::caution
Når du bruker `--hostname 0.0.0.0`, sett `OVERCODE_SERVER_PASSWORD` for å sikre serveren.

```bash
OVERCODE_SERVER_PASSWORD=your-password overcode serve --hostname 0.0.0.0
```

:::

---

## Web-klient + WSL

For best web-opplevelse på Windows:

1. **Kjør `overcode web` i WSL-terminalen** i stedet for PowerShell:

   ```bash
   overcode web --hostname 0.0.0.0
   ```

2. **Åpne i Windows-nettleseren** på `http://localhost:<port>` (Overcode skriver ut URL-en)

Når du kjører `overcode web` fra WSL, får du riktig filsystemtilgang og terminalintegrasjon, samtidig som det er tilgjengelig fra Windows-nettleseren din.

---

## Tilgang til Windows-filer

WSL kan få tilgang til alle Windows-filer via `/mnt/`-katalogen:

- `C:`-stasjon → `/mnt/c/`
- `D:`-stasjon → `/mnt/d/`
- Og så videre...

Eksempel:

```bash
cd /mnt/c/Users/YourName/Documents/project
overcode
```

:::tip
For en jevnere opplevelse kan du klone/kopiere repoet ditt inn i WSL-filsystemet (for eksempel under `~/code/`) og kjøre Overcode der.
:::

---

## Tips

- Kjør Overcode i WSL for prosjekter lagret på Windows-stasjoner: filtilgang fungerer smidig
- Bruk [WSL-utvidelsen i VS Code](https://code.visualstudio.com/docs/remote/wsl) sammen med Overcode for en integrert utviklingsflyt
- Overcode-konfigurasjon og sesjoner lagres i WSL-miljøet på `~/.local/share/overcode/`
